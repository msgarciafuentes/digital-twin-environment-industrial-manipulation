# Meshes

Place 3D mesh files (STL, OBJ) for robot models and environment objects here.

MuJoCo supports the following mesh formats:
- **STL** (`.stl`) — binary or ASCII
- **OBJ** (`.obj`) — Wavefront format

Meshes referenced in MJCF XML model files are resolved relative to the `meshdir` compiler attribute.
